# Deployment requirements — this is the file Vercel installs when building
# the Flask backend (backend/app.py). Keep it minimal: Vercel serverless
# functions have a 250 MB size limit, so heavy training/notebook packages
# live in requirements-dev.txt instead.
#
# For local ML development use: pip install -r requirements-dev.txt

Flask>=3.0.0
flask-cors>=4.0.0
python-dotenv>=1.0.0
joblib>=1.4.0
numpy>=1.26.0
# Model .pkl files in backend/models were pickled with scikit-learn 1.9.0;
# older versions may fail to unpickle them.
scikit-learn>=1.9.0